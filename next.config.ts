import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // This is to load the app through /resumepro
  // basePath: "/resumepro",
  // assetPrefix: "/resumepro",
  // output: "standalone",
};

export default nextConfig;
